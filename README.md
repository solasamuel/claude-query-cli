# claude-query-cli
claude-query is a CLI tool that connects to any structured data source — a PostgreSQL database, a CSV file, a Notion database, an Airtable base, a JSON file — and lets you query it in plain English. It uses Claude's tool-use API to construct and execute the appropriate query, then returns formatted results with the query it ran shown transparently.
